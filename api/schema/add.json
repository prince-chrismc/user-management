{
   "$schema": "http://json-schema.org/draft-07/schema",
   "$id": "user.json",
   "type": "object",
   "title": "The User Schema",
   "description": "The add schema is the schema that comprises the fields of a user to be created.",
   "required": [
       "name",
       "email"
   ],
   "properties": {
       "name": {
           "$id": "#/properties/name",
           "type": "string",
           "title": "The Name Schema",
           "description": "the name given to a user.",
           "examples": [
               "John Doe",
                "Jane Doe"
           ]
       },
       "email": {
           "$id": "#/properties/email",
           "type": "string",
           "title": "The Email Schema",
           "description": "The means of contact for a given user.",
           "examples": [
               "test.user@example.com"
           ]
       }
   }
}
